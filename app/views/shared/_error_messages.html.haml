- if @word.errors.any?
  #error_explanation
    .alert.alert-danger
      %ul
        - @word.errors.full_messages.each do |msg|
          %li= msg


-# error message without the field_names
  - @object.errors.messages.values.each do |msg|
    - msg.each do  |m|
      %li= m